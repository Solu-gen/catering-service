import React from 'react';
import ReactDOM from 'react-dom/client';
import FOF from './pages/FOF';

ReactDOM.createRoot(document.getElementById('root_fof')).render(
  <React.StrictMode>
    <FOF />
  </React.StrictMode>
);