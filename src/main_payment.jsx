import React from 'react';
import ReactDOM from 'react-dom/client';
import PaymentPage from './pages/PaymentPage';

ReactDOM.createRoot(document.getElementById('root_pay')).render(
  <React.StrictMode>
    <PaymentPage />
  </React.StrictMode>
);