import React from 'react';
import ReactDOM from 'react-dom/client';
import ProfilePage from './pages/ProfilePage';

ReactDOM.createRoot(document.getElementById('root_acc')).render(
  <React.StrictMode>
    <ProfilePage />
  </React.StrictMode>
);