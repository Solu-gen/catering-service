

import React from 'react';
import ReactDOM from 'react-dom/client';
import CartPage from './pages/CartPage';

ReactDOM.createRoot(document.getElementById('root_cart')).render(
  <React.StrictMode>
    <CartPage />
  </React.StrictMode>
);