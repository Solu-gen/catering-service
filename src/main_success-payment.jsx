import React from 'react';
import ReactDOM from 'react-dom/client';
import PaymentSuccessPage from './pages/PaymentSuccessPage';

ReactDOM.createRoot(document.getElementById('root_succ')).render(
  <React.StrictMode>
    <PaymentSuccessPage />
  </React.StrictMode>
);